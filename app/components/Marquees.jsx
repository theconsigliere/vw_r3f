export default function Marquees({ title }) {
  return (
    <li className="marquees__travel mq">
      <span className="marquees__content">{title}</span>
      <span className="marquees__content">{title}</span>
      <span className="marquees__content">{title}</span>
      <span className="marquees__content">{title}</span>
      <span className="marquees__content">{title}</span>
      <span className="marquees__content">{title}</span>
      <span className="marquees__content">{title}</span>
      <span className="marquees__content">{title}</span>
    </li>
  )
}
